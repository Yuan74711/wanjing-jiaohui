# 2026-08-15

## 完成：《万镜教会》ARG 网页游戏（纯静态，无后端）

项目目录：`c:\Users\Administrator\WorkBuddy\2026-08-15-20-16-57\`

- 38 个 HTML 页面 + 4 CSS + 4 JS（共 46 文件），5 章节，约 40 页目标达成。
- 核心诡计：玩家以为是调查邪教，实则在完成「第13次镜面仪式」，玩家即第13祭品。
- 结构：根目录(暖色·兔子洞) / forum/(银灰·13节点) / realworld/(黑紫·现实解谜) / backend/(黑紫·反转) / endings/(3结局) / hidden/(4隐藏页) / punishment.html(F12彩蛋)。
- 共享机制：
  - `js/progress.js`：Progress 对象，localStorage 记 progress(0-13)/currentChapter/playerId；`getId()` 随机工号 `AUD-xxxx`；底部渲染 13 面镜子进度条。
  - `js/search.js`：统一搜索路由 `search.html?q=`，关键词→页面（万镜教会/镜主→forum，镜子→post_01，塔/坐标/街景/监视/后台/证据/1999/红房间/转录/最后一面镜子 等）；提交时存 `localStorage.lastQuery` 供 surveillance 页读取。
  - `js/audio.js`：Drone Web Audio 低频嗡鸣 + `shatter`/`crack` 玻璃碎裂，音量随章节渐增(18-52Hz / 0.04-0.28)。
  - `js/f12.js`：任意页 F12→punishment.html。
- 多结局：`endings/reject.html`(假结局)/`accept.html`(真结局A·招募入口)/`mirror_exit.html`(真结局B·打破第四面墙·1999 ARG·13观众失踪)，由 backend/auditor.html 输入框/三按钮按 拒绝/接受/MIRROR_EXIT 分流。
- 伏笔闭环：镜字(页脚异常字→404)、13(13节点/13观众/第13祭品)、口号(每页反复出现)、工号(Progress.getId 贯穿 street/auditor/1999/mirror)、1999(about→hidden/1999→transcript)。
- auditor.html 用 beforeunload + history.pushState 锁定刷新/关闭/后退；locked.html 中转。

运行建议：用本地静态服务器（如 `python -m http.server`）打开 index.html，避免 file:// 下 localStorage/AudioContext 限制；音效需用户首次交互后才会响。

## 通篇逻辑理顺（第二轮修复）
审查发现并修复 5 处断裂/不一致：
1. **search.js 跨目录死链（核心 bug）**：子目录页内嵌搜索框提交后 `location.href` 按当前目录解析，导致 `forum/forum/index.html` 类死链。修复：新增 `rootPrefix()`（按 pathname 层级算 `../` 前缀），submit 与 `?q=` 直达跳转均补前缀。ROUTES 目标统一相对站点根。
2. **404.html 摩斯错误**：原串 `-- .. .-. .-. --- .- -. -.- . .- .-..` 解不出 MIRROR，改为 `-- .. .-. .-. --- .-.`（= MIRROR = 万镜教会）。
3. **tower.html 摩斯错误**：原串解不出镜主之名，改为同一 MIRROR 摩斯，提示"六个字母 MIRROR=镜主名字"。
4. **coordinates.html 提示搜「街」**：search.js 原无 `街` 路由，已补 `'街':'realworld/street.html'`（与 `街景` 并存）。
5. **post_mirror_02 Base64 乱码**：原串解出"登定先册/缺记"错字，重生成正确中文 Base64（解码=「你已被登记。一个早就写进名册的人，不必惊讶。」）。
验证：f12.js 跨目录跳转、forum 各帖 01→02→…→13→auditor 链接、post_11→realworld→evidence→auditor 支线均正常；post_09 Hex E9 95 9C=镜 正确。
主线流程已闭环：index(点镜)→404(摩斯=MIRROR)→搜万镜教会→forum 13帖→11/坐标支线→evidence→auditor(锁定)→拒绝/接受/MIRROR_EXIT→3结局→hidden 页。

## 弱化提示、增强探索（第三轮）
按用户要求减少"手把手"剧透，让玩家靠谜题+搜索框自行探索：
- forum 帖子 01~12 移除"进入第X面镜子 →"直链，改弱暗示"镜子会自己领你走，回到论坛"；post_13 后台链接弱化为低调文字"内容管理后台"。
- realworld/coordinates 移除"下一步"ul(street/tower直链)与"试试搜这些词:塔/街景/监视/证据/后台"答案清单，坐标读数去掉具体数字答案(2318→8132)；street/tower/surveillance 移除"下一步"ul直链改模糊暗示；evidence 大按钮"进入后台"弱化为低调链接。
- 404 去掉"输入那四个字"明示，改"把电码交给镜子"；about 去掉"站内搜索:1999"直链改"去问问镜子，它记得1999"；forum/index 置顶引导"从第一面开始"改"十三面镜子都在，你愿意点几面"。
- 保留必要入口钩子：index 搜索提示、404 摩斯+搜索链接、auditor 结局输入框提示(拒绝/接受/MIRROR_EXIT)、post_13 与 evidence 低调"后台"链接，确保路径全部可达不死链。
- grep 复核：html 中已无"进入第/下一步/坐标解谜/登录内容管理后台/进入后台▸/收集证据"等用户可见剧透直链（仅 evidence.js 注释"标记进入第四章"残留，非可见）。
